import { AnimateNumberPipe } from './animate-number.pipe';

describe('AnimateNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new AnimateNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
